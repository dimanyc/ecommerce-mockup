<%@cart.line_items.each do |i| %>
<%=i.quantity%> &times; <%=Product.find(i.product_id).title%> | <%=number_to_currency(Product.find(i.product_id).price)%><br/>
<%end%>

<%= number_to_currency(cart.total_price) %>

<%= button_to 'Checkout', new_card_path, method: :get  %> 
<br/>
<%= link_to 'Clear Cart', cart_path,method: :delete,
data: { confirm: 'Are you sure?' }%> &nbsp;|&nbsp;

<%= link_to "Continue Shopping", home_path %>
